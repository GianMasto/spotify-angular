import { RemoveRepeatedFromListPipe } from './remove-repeated-from-list.pipe';

describe('RemoveRepeatedFromListPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveRepeatedFromListPipe();
    expect(pipe).toBeTruthy();
  });
});
